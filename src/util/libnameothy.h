#include <libgen.h>

#include <cctype>
#include <sstream>
#include <string>
#include <utility>
#include <vector>

// in a REAL project this would be in a namespace
std::pair<std::string, std::string> libnameothy(std::string &path);
